# Local history / state
.downterm_history
.downterm_last.txt
.downterm_archive.txt
*.part
*.temp

# OS junk
Thumbs.db
desktop.ini
.DS_Store

# Downloaded videos (keep binaries, ignore output)
*.mp4
*.mkv
*.webm
*.mp3
*.m4a
*.f*
*.part
*.temp

# Bundled binaries - shipped in repo + release zips for the Windows full bundle.
# (Linux/macOS users still fetch via `s`/setup. See bin/checksums.txt for pinned versions.)
# Binaries are tracked. Nothing to ignore here.

# Agent handoff (local only — not for git)
agent.md
AGENTS.md
AGENT_HANDOFF.md
