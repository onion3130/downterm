# Local history / state
.downterm_history
.downterm_last.txt
.downterm_archive.txt
*.part
*.temp

# OS junk
Thumbs.db
desktop.ini
.DS_Store

# Downloaded videos (keep binaries, ignore output)
*.mp4
*.mkv
*.webm
*.mp3
*.m4a
*.f*
*.part
*.temp

# Bundled binaries - fetched on first run via `s`/setup command.
# Not tracked in repo. See bin/checksums.txt for pinned versions.
yt-dlp.exe
yt-dlp
ffmpeg.exe
ffmpeg
deno.exe
deno

# Agent handoff file (not for git)
AGENT_HANDOFF.md
