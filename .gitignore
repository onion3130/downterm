# Local history / state
.downterm_history
.downterm_last.txt
.downterm_archive.txt
.downterm_path_ok
.downterm_playlist
downterm.conf
*.part
*.temp

# OS junk
Thumbs.db
desktop.ini
.DS_Store

# Downloaded videos (keep binaries, ignore output)
*.mp4
*.mkv
*.webm
*.mp3
*.m4a
*.f*
*.part
*.temp

# Local tool binaries — setup downloads these (yt-dlp ~17MB, ffmpeg ~98MB, deno ~93MB).
# Not tracked; never commit platform binaries to the repo.
yt-dlp.exe
ffmpeg.exe
deno.exe

# Agent handoff (local only — not for git)
agent.md
AGENTS.md
AGENT_HANDOFF.md
